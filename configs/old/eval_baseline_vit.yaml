seed: 42
deterministic: true

model: vit_base_patch16_224
num_classes: 2
pretrained: true
freeze_layers: 0  # optional; irrelevant if not training, but harmless

batch_size: 32
num_workers: 4
val_split: 0.1
test_split: 0.1
robustness_split: 0.1

checkpoint_path: null

output_dir: reports/eval_pretrained_vit
save_predictions: true
save_confusion_matrix: true
use_mlflow: false